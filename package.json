{
  "name": "Event_Manager",
  "version": "1.0.0",
  "workspaces": [
    "app",
    "database"
  ],
  "dependencies": {
    "@fortawesome/react-fontawesome": "^0.2.3",
    "cp": "^0.2.0",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "app": "npm run dev --workspace=app",
    "database": "npm run dev --workspace=database",
    "dev": "npm-run-all --parallel app database"
  }
}
